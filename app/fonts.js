import { DM_Sans, Manrope } from "next/font/google";

export const DMSans = DM_Sans({
  subsets: ["latin"],
  display: "swap",
});

export const manrope = Manrope({
  subsets: ["latin"],
  display: "swap",
});
